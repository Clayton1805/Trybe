const objeto = {
  chave1: 'Clayton',
  chave2: 'Zica'
}

objeto.chave OU objeto['chave']// retorna o valor da chave do objeto.

objeto.chave OU objeto['chave'] = "valorDaChave"; // igualando os valores dos objetos da forma mostrada você não só pode reatribuir os valores das chaves do objeto como também caso coloque na frente do objeto uma chave que não exista ele ira criala contanto que você a iguale a um valor.

Object.entries(objeto) // retorna um array de arrays com [key, value]


